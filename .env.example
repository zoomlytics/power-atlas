# Backend Configuration
BACKEND_PORT=8000

# Frontend Configuration
FRONTEND_PORT=3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Neo4j Configuration (copy to .env, which should stay gitignored, and replace before running Docker Compose)
# Use a strong password (e.g., 12+ chars, mix upper/lowercase, numbers, and symbols).
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=CHANGE_ME_BEFORE_USE
# Review Neo4j license (https://neo4j.com/licensing/) and GDS license (https://neo4j.com/graph-data-science-software/)
# before setting this to 'yes'.
NEO4J_ACCEPT_LICENSE_AGREEMENT=CHANGE_ME_TO_YES_AFTER_REVIEWING_LICENSE
# WARNING: Granting unrestricted access to procedures can be risky and should not be used in production.
# Defaulting to gds.* so the README verification step works for local graph experiments; clear or tighten for hardened envs.
NEO4J_UNRESTRICTED_PROCS=gds.*
