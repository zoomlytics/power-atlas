# Archive Notice

`_archive/` contains historical experimentation material that is kept for reference only.

This directory is:

- not part of the active Power Atlas product or pipeline surface,
- not the supported operator walkthrough path,
- not the package-native implementation target for ongoing restructure work.

The active implementation and walkthrough surfaces remain under `demo/`, `src/power_atlas/`, and the current repository documentation.

Content under `_archive/` should be treated as historical context unless a document explicitly states otherwise.