........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
........................................................................ [ 13%]
........................................................................ [ 16%]
........................................................................ [ 20%]
...........................FFFFFFFF........FFF.....FFFFFF.......FF.F.... [ 23%]
.............................F...............F........F....FFF.F..... [ 26%]
...F......FFFFFFF..F..................FFFFFFFFFFFFFFFFFFFFFFFFF......... [ 29%]
FFFFFFFFFFFF............................................................ [ 33%]
........................................................................ [ 36%]
........................................................................ [ 39%]
........................................................................ [ 43%]
........................................................................ [ 46%]
........................................................................ [ 50%]
....................................................F................... [ 53%]
........................................................................ [ 56%]
........................................................................ [ 60%]
........................................................................ [ 63%]
........................................................................ [ 66%]
........................................................................ [ 70%]
........................................................................ [ 73%]
........................................................................ [ 76%]
........................................................................ [ 80%]
........................................................................ [ 83%]
......................FF...............FF..FFFF......................... [ 86%]
........................................................................ [ 90%]
........................................................................ [ 93%]
........................................................................ [ 96%]
....................................................................     [100%]
=================================== FAILURES ===================================
_ TestEntityResolutionRequestContextLive.test_live_empty_mentions_returns_zero_counts _

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_empty_mentions_returns_zero_counts>

    def test_live_empty_mentions_returns_zero_counts(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            driver = self._make_driver([], [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-007",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:915: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
____ TestEntityResolutionRequestContextLive.test_live_resolves_alias_match _____

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_resolves_alias_match>

    def test_live_resolves_alias_match(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [{"mention_id": "m3", "name": "D. Adams", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q42", "run_id": "run-s1", "name": "Douglas Adams", "aliases": "D. Adams|Adams", "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-003",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:810: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
____ TestEntityResolutionRequestContextLive.test_live_resolves_label_match _____

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_resolves_label_match>

    def test_live_resolves_label_match(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [{"mention_id": "m2", "name": "Douglas Adams", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q42", "run_id": "run-s1", "name": "Douglas Adams", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-002",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:792: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_____ TestEntityResolutionRequestContextLive.test_live_resolves_qid_match ______

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_resolves_qid_match>

    def test_live_resolves_qid_match(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Q42", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q42", "run_id": "run-s1", "name": "Douglas Adams", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-001",
                    source_uri="file:///doc.pdf",
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:771: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_____ TestEntityResolutionRequestContextLive.test_live_unresolved_fallback _____

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_unresolved_fallback>

    def test_live_unresolved_fallback(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [{"mention_id": "m4", "name": "Nobody Known", "entity_type": None}]
            canonicals = [{"entity_id": "Q42", "run_id": "run-s1", "name": "Douglas Adams", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-004",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:848: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextLive.test_live_uses_config_dataset_name_when_dataset_id_omitted _

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_uses_config_dataset_name_when_dataset_id_omitted>

    def test_live_uses_config_dataset_name_when_dataset_id_omitted(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = dataclass_replace(_live_config(Path(tmpdir)), dataset_name="demo_dataset_v2")
            mentions = [{"mention_id": "m3b", "name": "Douglas Adams", "entity_type": "person"}]
            canonicals = [
                {"entity_id": "Q84", "run_id": "run-s2", "name": "Douglas Adams", "aliases": None, "dataset_id": "demo_dataset_v2"}
            ]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-config-dataset-001",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:830: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestEntityResolutionRequestContextLive.test_live_writes_summary_artifact ___

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_writes_summary_artifact>

    def test_live_writes_summary_artifact(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m6", "name": "Q1", "entity_type": None},
                {"mention_id": "m7", "name": "Unknown", "entity_type": None},
            ]
            canonicals = [{"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-006",
                    source_uri="file:///a.pdf",
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:892: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextLive.test_live_writes_unresolved_artifact __

self = <test_entity_resolution.TestEntityResolutionRequestContextLive testMethod=test_live_writes_unresolved_artifact>

    def test_live_writes_unresolved_artifact(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [{"mention_id": "m5", "name": "Mystery Person", "entity_type": None}]
            canonicals: list[dict[str, Any]] = []
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-live-005",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:866: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_clusters_created_counts_unique_entity_type_and_text_pairs _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_clusters_created_counts_unique_entity_type_and_text_pairs>

    def test_clusters_created_counts_unique_entity_type_and_text_pairs(self):
        """clusters_created treats same text with different entity types as separate clusters."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            # "IBM" appears as both ORG and PRODUCT — should create 2 distinct clusters.
            mentions = [
                {"mention_id": "m1", "name": "IBM", "entity_type": "ORG"},
                {"mention_id": "m2", "name": "IBM", "entity_type": "PRODUCT"},
            ]
            driver = _make_neo4j_test_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-scope-002",
                    source_uri=None,
                    resolution_mode="unstructured_only",
                )

demo/tests/test_entity_resolution.py:1470: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_clusters_created_none_and_empty_entity_type_counted_as_one _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_clusters_created_none_and_empty_entity_type_counted_as_one>

    def test_clusters_created_none_and_empty_entity_type_counted_as_one(self):
        """None and empty-string entity_type must be treated as the same cluster scope."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            # Both mentions have the same normalized text; one has entity_type=None,
            # the other has entity_type="".  They must map to the same cluster_id
            # and clusters_created must be 1, not 2.
            mentions = [
                {"mention_id": "m1", "name": "Acme", "entity_type": None},
                {"mention_id": "m2", "name": "Acme", "entity_type": ""},
            ]
            driver = _make_neo4j_test_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-scope-003",
                    source_uri=None,
                    resolution_mode="unstructured_only",
                )

demo/tests/test_entity_resolution.py:1497: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
____ TestResolvedEntityCluster.test_cross_run_clusters_created_are_isolated ____

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_cross_run_clusters_created_are_isolated>

    def test_cross_run_clusters_created_are_isolated(self):
        """Two separate runs with the same mentions produce independent clusters."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Acme Corp", "entity_type": "ORG"}]
    
            with patch("neo4j.GraphDatabase.driver", return_value=_make_neo4j_test_driver(mentions, [])):
>               result_run1 = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-iso-001",
                    source_uri=None,
                    resolution_mode="unstructured_only",
                )

demo/tests/test_entity_resolution.py:1517: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_live_clusters_created_equals_unique_normalized_texts _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_clusters_created_equals_unique_normalized_texts>

    def test_live_clusters_created_equals_unique_normalized_texts(self):
        """Two mentions with the same normalized text map to ONE cluster."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [
                {"mention_id": "m1", "name": "Mystery Corp", "entity_type": None},
                {"mention_id": "m2", "name": "mystery corp", "entity_type": None},  # same normalized
                {"mention_id": "m3", "name": "Other Entity", "entity_type": None},
            ]
            driver = self._make_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1128: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_live_clusters_created_is_zero_when_all_resolved _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_clusters_created_is_zero_when_all_resolved>

    def test_live_clusters_created_is_zero_when_all_resolved(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Q42", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q42", "run_id": "run-s1", "name": "Douglas Adams", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-002",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:1150: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
__ TestResolvedEntityCluster.test_live_member_of_edge_written_for_unresolved ___

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_member_of_edge_written_for_unresolved>

    def test_live_member_of_edge_written_for_unresolved(self):
        """Verify the MEMBER_OF Cypher is invoked for unresolved mentions."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Nobody Known", "entity_type": None}]
            driver = self._make_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-005",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1222: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestResolvedEntityCluster.test_live_resolved_entity_cluster_node_merged ____

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_resolved_entity_cluster_node_merged>

    def test_live_resolved_entity_cluster_node_merged(self):
        """Verify ResolvedEntityCluster label appears in the Cypher for unresolved mentions."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Widget Co", "entity_type": None}]
            driver = self._make_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-006",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1248: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_____ TestResolvedEntityCluster.test_live_summary_carries_cluster_version ______

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_summary_carries_cluster_version>

    def test_live_summary_carries_cluster_version(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            driver = self._make_driver([], [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-003",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1170: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_live_unresolved_artifact_includes_cluster_id __

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_live_unresolved_artifact_includes_cluster_id>

    def test_live_unresolved_artifact_includes_cluster_id(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Widget Inc", "entity_type": None}]
            driver = self._make_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cluster-004",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1194: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_per_mention_source_uri_produces_same_cluster_id_in_artifact _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_per_mention_source_uri_produces_same_cluster_id_in_artifact>

    def test_per_mention_source_uri_produces_same_cluster_id_in_artifact(self):
        """Mentions from different sources within the same run_id produce the SAME cluster_id
        in the unresolved artifact — source_uri is provenance on edges, not cluster identity."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            # Two mentions with identical text and entity_type, but different source_uri
            # stored on the EntityMention node in the DB.
            mentions = [
                {"mention_id": "m1", "name": "IBM", "entity_type": "ORG", "source_uri": "https://example.com/doc1.pdf"},
                {"mention_id": "m2", "name": "IBM", "entity_type": "ORG", "source_uri": "https://example.com/doc2.pdf"},
            ]
            driver = _make_neo4j_test_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-src-scope-001",
                    source_uri=None,
                    resolution_mode="unstructured_only",
                )

demo/tests/test_entity_resolution.py:1326: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestResolvedEntityCluster.test_per_mention_source_uri_retained_on_member_of_edges _

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_per_mention_source_uri_retained_on_member_of_edges>

    def test_per_mention_source_uri_retained_on_member_of_edges(self):
        """Per-mention source_uri from different source documents is preserved as provenance
        on MEMBER_OF edge rows even when both mentions map to the same cluster_id.
    
        This verifies the provenance retention guarantee: source_uri is NOT part of
        cluster identity but IS carried per-mention on MEMBER_OF edges so that origin
        tracking is preserved without forcing source-partitioned cluster identity.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [
                {"mention_id": "m1", "name": "IBM", "entity_type": "ORG", "source_uri": "https://example.com/doc1.pdf"},
                {"mention_id": "m2", "name": "IBM", "entity_type": "ORG", "source_uri": "https://example.com/doc2.pdf"},
            ]
            driver = _make_neo4j_test_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-provenance-001",
                    source_uri=None,
                    resolution_mode="unstructured_only",
                )

demo/tests/test_entity_resolution.py:1368: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestResolvedEntityCluster.test_unresolved_artifact_includes_entity_type ____

self = <test_entity_resolution.TestResolvedEntityCluster testMethod=test_unresolved_artifact_includes_entity_type>

    def test_unresolved_artifact_includes_entity_type(self):
        """Unresolved artifact rows expose entity_type for downstream consumers."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _make_config(
                dry_run=False,
                output_dir=Path(tmpdir),
                password="secret",
            )
            mentions = [{"mention_id": "m1", "name": "Acme Corp", "entity_type": "ORG"}]
            driver = _make_neo4j_test_driver(mentions, [])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-scope-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:1277: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestEntityTypeDriftReport.test_live_summary_includes_entity_type_report ____

self = <test_entity_resolution.TestEntityTypeDriftReport testMethod=test_live_summary_includes_entity_type_report>

    def test_live_summary_includes_entity_type_report(self):
        """live summary must include an entity_type_report that reflects observed mentions."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "IBM", "entity_type": "ORG"},
                {"mention_id": "m2", "name": "Alice", "entity_type": "Person"},
                {"mention_id": "m3", "name": "Mystery", "entity_type": None},
            ]
            canonicals: list[dict] = []
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="drift-live-001",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:1899: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityTypeDriftReport.test_sentinel_label_collision_propagates_to_stage_warnings _

self = <test_entity_resolution.TestEntityTypeDriftReport testMethod=test_sentinel_label_collision_propagates_to_stage_warnings>

    def test_sentinel_label_collision_propagates_to_stage_warnings(self):
        """sentinel_label_warnings from entity_type_report must appear in top-level warnings.
    
        Regression test: when an upstream extractor emits the reserved sentinel
        label '__null__' alongside absent/empty mentions, the entity-resolution
        stage must propagate the resulting sentinel_label_warnings into the stage's
        top-level 'warnings' list so the collision is visible at orchestration
        boundaries.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = _live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Acme"},  # entity_type absent → None
                {"mention_id": "m2", "name": "Weird", "entity_type": "__null__"},  # reserved sentinel
            ]
            canonicals: list[dict] = []
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="drift-sentinel-001",
                    source_uri=None,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:1934: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_abbreviation_method_writes_candidate_status _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_abbreviation_method_writes_candidate_status>

    def test_abbreviation_method_writes_candidate_status(self):
        """abbreviation cluster assignments must write status='candidate'.
    
        Abbreviated forms are inherently ambiguous (e.g. 'FBI' could match multiple
        long forms), so they are classified as 'candidate' rather than 'provisional'
        to make the ambiguity explicit for downstream consumers and reviewers.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Federal Bureau of Investigation",
                 "entity_type": "organization"},
                {"mention_id": "m2", "name": "fbi",
                 "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-status-003",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2671: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_borderline_fuzzy_writes_review_required_status _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_borderline_fuzzy_writes_review_required_status>

    def test_borderline_fuzzy_writes_review_required_status(self):
        """Borderline fuzzy matches (ratio < _FUZZY_REVIEW_THRESHOLD) must write status='review_required'.
    
        'European Central Bank' vs 'Euro Central Bank' has a similarity of ~0.895,
        which is above the fuzzy entry threshold (0.85) but below _FUZZY_REVIEW_THRESHOLD
        (0.92), so the membership is classified as 'review_required'.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "European Central Bank",
                 "entity_type": "organization"},
                {"mention_id": "m2", "name": "Euro Central Bank",
                 "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-status-005",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2705: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_candidate_match_edges_written_for_abbreviation _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_candidate_match_edges_written_for_abbreviation>

    def test_candidate_match_edges_written_for_abbreviation(self):
        """CANDIDATE_MATCH edges must be written for 'candidate' (abbreviation) memberships."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Federal Bureau of Investigation",
                 "entity_type": "organization"},
                {"mention_id": "m2", "name": "fbi",
                 "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cand-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2743: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_candidate_match_edges_written_for_borderline_fuzzy _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_candidate_match_edges_written_for_borderline_fuzzy>

    def test_candidate_match_edges_written_for_borderline_fuzzy(self):
        """CANDIDATE_MATCH edges must be written for 'review_required' (borderline fuzzy) memberships."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "European Central Bank",
                 "entity_type": "organization"},
                {"mention_id": "m2", "name": "Euro Central Bank",
                 "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cand-002",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2770: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_deterministic_methods_write_accepted_status _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_deterministic_methods_write_accepted_status>

    def test_deterministic_methods_write_accepted_status(self):
        """label_cluster and normalized_exact must write status='accepted'."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                # m1: singleton → label_cluster for "uniquename1"
                {"mention_id": "m1", "name": "UniqueName1", "entity_type": "person"},
                # m2: duplicate of m1 → normalized_exact for "uniquename1"
                {"mention_id": "m2", "name": "UniqueName1", "entity_type": "person"},
                # m3: distinct singleton → label_cluster for "uniquename2"
                {"mention_id": "m3", "name": "UniqueName2", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-status-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2600: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_fuzzy_method_writes_provisional_status _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_fuzzy_method_writes_provisional_status>

    def test_fuzzy_method_writes_provisional_status(self):
        """High-confidence fuzzy matches (ratio >= _FUZZY_REVIEW_THRESHOLD) write status='provisional'.
    
        'Federal Reserve Board' vs 'Federal Reserve Boards' has a SequenceMatcher ratio
        of ~0.977 which is above _FUZZY_REVIEW_THRESHOLD=0.92, so the membership is
        classified as 'provisional' (minor surface-form variant, review optional).
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            # Two mentions that fuzzy-match each other (same type, similar text)
            mentions = [
                {"mention_id": "m1", "name": "Federal Reserve Board",
                 "entity_type": "organization"},
                {"mention_id": "m2", "name": "Federal Reserve Boards",
                 "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-status-002",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2637: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_____ TestClusterMentionsUnstructuredOnly.test_live_all_mentions_clustered _____

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_all_mentions_clustered>

    def test_live_all_mentions_clustered(self):
        """In unstructured_only mode all mentions end up in clusters (unresolved)."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-002",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2421: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
______ TestClusterMentionsUnstructuredOnly.test_live_clustering_invariant ______

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_clustering_invariant>

    def test_live_clustering_invariant(self):
        """mentions_clustered + mentions_unclustered == mentions_total (graph-backed invariant)."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
                {"mention_id": "m3", "name": "Charlie", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-invariant-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2464: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_live_mentions_clustered_equals_mentions_total _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_mentions_clustered_equals_mentions_total>

    def test_live_mentions_clustered_equals_mentions_total(self):
        """In unstructured_only mode mentions_clustered == mentions_total and mentions_unclustered == 0."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
                {"mention_id": "m3", "name": "Charlie", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-clustered-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2443: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
______ TestClusterMentionsUnstructuredOnly.test_live_no_canonical_lookup _______

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_no_canonical_lookup>

    def test_live_no_canonical_lookup(self):
        """unstructured_only should NOT touch CanonicalEntity nodes at all."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Alice", "entity_type": "person"}]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2398: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_live_normalized_exact_reduces_clusters _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_normalized_exact_reduces_clusters>

    def test_live_normalized_exact_reduces_clusters(self):
        """Two mentions with the same normalized text -> one cluster."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": None},
                {"mention_id": "m2", "name": "ALICE", "entity_type": None},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-003",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2504: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_live_resolution_breakdown_no_structured_methods _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_resolution_breakdown_no_structured_methods>

    def test_live_resolution_breakdown_no_structured_methods(self):
        """Resolution breakdown should not contain qid_exact/label_exact/alias_exact."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Widget Corp", "entity_type": None},
                {"mention_id": "m2", "name": "Widget Corp.", "entity_type": None},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-005",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2537: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestClusterMentionsUnstructuredOnly.test_live_resolution_mode_in_summary ___

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_live_resolution_mode_in_summary>

    def test_live_resolution_mode_in_summary(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            driver = self._make_driver([])
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-uo-004",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2518: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestClusterMentionsUnstructuredOnly.test_no_candidate_match_edges_for_accepted_memberships _

self = <test_entity_resolution.TestClusterMentionsUnstructuredOnly testMethod=test_no_candidate_match_edges_for_accepted_memberships>

    def test_no_candidate_match_edges_for_accepted_memberships(self):
        """CANDIDATE_MATCH edges must NOT be written for deterministic ('accepted') memberships."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            # Use names with no textual similarity so they each form singletons via
            # label_cluster (deterministic, status='accepted') and cannot fuzzy-match.
            mentions = [
                {"mention_id": "m1", "name": "Alpha Corp", "entity_type": "organization"},
                {"mention_id": "m2", "name": "Zeta LLC", "entity_type": "organization"},
            ]
            driver = self._make_driver(mentions)
    
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="run-cand-003",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2794: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_aligned_with_edge_written_for_label_match _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_aligned_with_edge_written_for_label_match>

    def test_live_aligned_with_edge_written_for_label_match(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Alice", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-004",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3030: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_aligned_with_written_for_alias_match _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_aligned_with_written_for_alias_match>

    def test_live_aligned_with_written_for_alias_match(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            # "ali" is an alias for "Alice" (Q1)
            mentions = [{"mention_id": "m1", "name": "Ali", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": "Ali|Alicia", "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-005",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3069: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_aligned_with_written_with_non_null_source_uri _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_aligned_with_written_with_non_null_source_uri>

    def test_live_aligned_with_written_with_non_null_source_uri(self):
        """ALIGNED_WITH edges must also be written correctly with a non-null source_uri."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Alice", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
            source_uri = "file:///test-doc.pdf"
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config, run_id="hybrid-live-004b", source_uri=source_uri
                )

demo/tests/test_entity_resolution.py:3048: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_alignment_breakdown_reflects_methods _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_alignment_breakdown_reflects_methods>

    def test_live_alignment_breakdown_reflects_methods(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "BC", "entity_type": "org"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"},
                {"entity_id": "Q2", "run_id": "run-s1", "name": "Bob Corp", "aliases": "BC", "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-006",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3090: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_alignment_version_in_summary _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_alignment_version_in_summary>

    def test_live_alignment_version_in_summary(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            driver = self._make_driver([], [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-010",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3304: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_all_mentions_become_unresolved _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_all_mentions_become_unresolved>

    def test_live_all_mentions_become_unresolved(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2975: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestEntityResolutionRequestContextHybrid.test_live_clustering_invariant ____

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_clustering_invariant>

    def test_live_clustering_invariant(self):
        """mentions_clustered + mentions_unclustered == mentions_total (graph-backed invariant)."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-invariant-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3150: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_clusters_pending_alignment_zero_when_all_align _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_clusters_pending_alignment_zero_when_all_align>

    def test_live_clusters_pending_alignment_zero_when_all_align(self):
        """clusters_pending_alignment == 0 when every cluster aligns to a canonical."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob Corp", "entity_type": "org"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"},
                {"entity_id": "Q2", "run_id": "run-s1", "name": "Bob Corp", "aliases": None, "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-cpa-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3245: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_distinct_canonical_entities_aligned _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_distinct_canonical_entities_aligned>

    def test_live_distinct_canonical_entities_aligned(self):
        """distinct_canonical_entities_aligned counts unique canonical entity IDs."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            # Two mentions both align to the same canonical entity Q1
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Ali", "entity_type": "person"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": "Ali", "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-dce-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3174: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_distinct_canonical_entities_aligned_multiple _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_distinct_canonical_entities_aligned_multiple>

    def test_live_distinct_canonical_entities_aligned_multiple(self):
        """distinct_canonical_entities_aligned counts multiple distinct canonical IDs."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob Corp", "entity_type": "org"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"},
                {"entity_id": "Q2", "run_id": "run-s1", "name": "Bob Corp", "aliases": None, "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-dce-002",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3198: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_does_not_create_resolves_to_edges _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_does_not_create_resolves_to_edges>

    def test_live_does_not_create_resolves_to_edges(self):
        """hybrid mode must not write RESOLVES_TO edges (mentions stay in clusters)."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Alice", "entity_type": "person"}]
            canonicals = [{"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"}]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-011",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3336: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
__ TestEntityResolutionRequestContextHybrid.test_live_member_of_edge_written ___

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_member_of_edge_written>

    def test_live_member_of_edge_written(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Widget Corp", "entity_type": None}]
            driver = self._make_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-002",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:2990: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_mentions_clustered_equals_mentions_total _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_mentions_clustered_equals_mentions_total>

    def test_live_mentions_clustered_equals_mentions_total(self):
        """In hybrid mode mentions_clustered == mentions_total and mentions_unclustered == 0."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Bob", "entity_type": "person"},
            ]
            driver = self._make_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-mc-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3132: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_mentions_in_aligned_clusters _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_mentions_in_aligned_clusters>

    def test_live_mentions_in_aligned_clusters(self):
        """mentions_in_aligned_clusters counts mentions in clusters with ALIGNED_WITH edges."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            # "alice" and "ALICE" normalize to the same cluster, aligned to Q1
            # "Unknown" forms a separate unaligned cluster
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "ALICE", "entity_type": "person"},
                {"mention_id": "m3", "name": "Unknown", "entity_type": "person"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-miac-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3221: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_no_aligned_clusters_when_no_canonical _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_no_aligned_clusters_when_no_canonical>

    def test_live_no_aligned_clusters_when_no_canonical(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [{"mention_id": "m1", "name": "Alice", "entity_type": "person"}]
            driver = self._make_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-003",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3004: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_resolution_mode_in_summary _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_resolution_mode_in_summary>

    def test_live_resolution_mode_in_summary(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            driver = self._make_driver([], [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-008",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3277: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_resolver_method_in_summary _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_resolver_method_in_summary>

    def test_live_resolver_method_in_summary(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            driver = self._make_driver([], [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-009",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3289: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestEntityResolutionRequestContextHybrid.test_live_unaligned_clusters_preserved _

self = <test_entity_resolution.TestEntityResolutionRequestContextHybrid testMethod=test_live_unaligned_clusters_preserved>

    def test_live_unaligned_clusters_preserved(self):
        """Clusters with no canonical match must still exist (MEMBER_OF written)."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": "Alice", "entity_type": "person"},
                {"mention_id": "m2", "name": "Unknown Corp", "entity_type": "org"},
            ]
            canonicals = [
                {"entity_id": "Q1", "run_id": "run-s1", "name": "Alice", "aliases": None, "dataset_id": "demo_dataset_v1"},
            ]
            driver = self._make_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="hybrid-live-007",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3112: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestManifestGraphConsistency.test_hybrid_aligned_clusters_nonzero_when_alignments_exist _

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_hybrid_aligned_clusters_nonzero_when_alignments_exist>

    def test_hybrid_aligned_clusters_nonzero_when_alignments_exist(self):
        """Manifest aligned_clusters must be > 0 when ALIGNED_WITH edges exist in the graph.
    
        This is the primary regression test for the live-run divergence where the
        graph held 23 ALIGNED_WITH edges but the manifest reported resolved=0 /
        unresolved=262 with no alignment metrics.  A failure here means the
        post-write ALIGNED_WITH query result is not being captured in the manifest
        (manifest summarization failure, not a graph-write failure).
        """
        n_mentions = 30
        n_aligned = 23
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_hybrid_config(Path(tmpdir))
            mentions = self._make_mentions(n_mentions)
            canonicals = self._make_canonicals(mentions, n_aligned)
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-hybrid-align-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3501: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_____ TestManifestGraphConsistency.test_hybrid_alignment_bounds_invariant ______

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_hybrid_alignment_bounds_invariant>

    def test_hybrid_alignment_bounds_invariant(self):
        """aligned_clusters <= clusters_created and mentions_in_aligned <= mentions_clustered."""
        n_mentions = 50
        n_aligned = 20
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_hybrid_config(Path(tmpdir))
            mentions = self._make_mentions(n_mentions)
            canonicals = self._make_canonicals(mentions, n_aligned)
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-hybrid-bounds-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3550: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
____ TestManifestGraphConsistency.test_hybrid_alignment_partition_invariant ____

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_hybrid_alignment_partition_invariant>

    def test_hybrid_alignment_partition_invariant(self):
        """aligned_clusters + clusters_pending_alignment == clusters_created."""
        n_mentions = 40
        n_aligned = 15
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_hybrid_config(Path(tmpdir))
            mentions = self._make_mentions(n_mentions)
            canonicals = self._make_canonicals(mentions, n_aligned)
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-hybrid-partition-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3578: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ TestManifestGraphConsistency.test_hybrid_all_mentions_clustered_at_scale ___

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_hybrid_all_mentions_clustered_at_scale>

    def test_hybrid_all_mentions_clustered_at_scale(self):
        """In hybrid mode all mentions must be in MEMBER_OF clusters."""
        n = 64
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_hybrid_config(Path(tmpdir))
            mentions = self._make_mentions(n)
            driver = _make_neo4j_test_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-hybrid-cluster-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3464: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestManifestGraphConsistency.test_hybrid_manifest_reflects_graph_at_live_run_scale _

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_hybrid_manifest_reflects_graph_at_live_run_scale>

    def test_hybrid_manifest_reflects_graph_at_live_run_scale(self):
        """End-to-end: manifest must accurately reflect graph state at a realistic live-run scale.
    
        This test uses a moderately sized synthetic dataset with:
          - all extracted mentions clustered (MEMBER_OF coverage)
          - a non-zero number of ALIGNED_WITH edges via provided canonical nodes
    
        The unstructured clustering algorithm merges similarly-named mentions via
        fuzzy matching, so the actual number of distinct clusters (and therefore
        aligned clusters) will be smaller than the raw mention count.  The key
        invariant is that the manifest must *not* report zero aligned_clusters when
        ALIGNED_WITH edges exist in the graph — that was the live-run bug.
    
        Assertion messages identify whether the failure is in graph-write execution
        or in manifest post-write query capture.
        """
        # Use a moderate number of mentions to keep test runtime reasonable while
        # still exercising the clustering and alignment logic at scale.
        n_mentions = 50
        # Provide canonical nodes for the first few mentions.  Due to fuzzy merging
        # some of these will correspond to the same cluster, so the resulting
        # aligned_clusters count will be <= n_canonical.  The critical property under
        # test is that aligned_clusters > 0, not its exact value.
        n_canonical = 5
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_hybrid_config(Path(tmpdir))
            mentions = self._make_mentions(n_mentions)
            canonicals = self._make_canonicals(mentions, n_canonical)
            driver = _make_neo4j_test_driver(mentions, canonicals)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-hybrid-live-scale-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3621: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestManifestGraphConsistency.test_unstructured_only_all_mentions_clustered_at_scale _

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_unstructured_only_all_mentions_clustered_at_scale>

    def test_unstructured_only_all_mentions_clustered_at_scale(self):
        """A moderate number of mentions must all appear in MEMBER_OF clusters and partition invariants must hold."""
        n = 64
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_unstructured_config(Path(tmpdir))
            mentions = self._make_mentions(n)
            driver = _make_neo4j_test_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-uo-scale-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3404: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestManifestGraphConsistency.test_unstructured_only_clusters_created_at_most_mentions_total _

self = <test_entity_resolution.TestManifestGraphConsistency testMethod=test_unstructured_only_clusters_created_at_most_mentions_total>

    def test_unstructured_only_clusters_created_at_most_mentions_total(self):
        """clusters_created can never exceed mentions_total."""
        n = 262
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_unstructured_config(Path(tmpdir))
            mentions = self._make_mentions(n)
            driver = _make_neo4j_test_driver(mentions, [])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="consistency-uo-clusters-001",
                    source_uri=None,
                )

demo/tests/test_entity_resolution.py:3440: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestHybridAlignmentCrossDatasetIsolation.test_no_cross_dataset_leakage_when_only_v1_exists _

self = <test_entity_resolution.TestHybridAlignmentCrossDatasetIsolation testMethod=test_no_cross_dataset_leakage_when_only_v1_exists>

    def test_no_cross_dataset_leakage_when_only_v1_exists(self):
        """When dataset_id=v2 and only v1 canonical entities exist, no alignment occurs."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            # Only v1 canonical node exists; v2 has none.
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-hybrid-003",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:4025: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestHybridAlignmentCrossDatasetIsolation.test_v2_canonical_query_is_scoped_by_dataset_id _

self = <test_entity_resolution.TestHybridAlignmentCrossDatasetIsolation testMethod=test_v2_canonical_query_is_scoped_by_dataset_id>

    def test_v2_canonical_query_is_scoped_by_dataset_id(self):
        """The CanonicalEntity lookup query must include dataset_id = v2 in its parameters."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v2",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V2_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-hybrid-002",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:3972: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestHybridAlignmentCrossDatasetIsolation.test_v2_hybrid_aligns_to_v2_canonical_not_v1_for_shared_qid _

self = <test_entity_resolution.TestHybridAlignmentCrossDatasetIsolation testMethod=test_v2_hybrid_aligns_to_v2_canonical_not_v1_for_shared_qid>

    def test_v2_hybrid_aligns_to_v2_canonical_not_v1_for_shared_qid(self):
        """v2 hybrid alignment must attach v2 clusters to v2 CanonicalEntity nodes.
    
        When both demo_dataset_v1 and demo_dataset_v2 contain a CanonicalEntity for
        the same QID (e.g. Q950419 / Mercado Libre), running hybrid resolution for v2
        must align v2 clusters to the v2 canonical entity, not the v1 one.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            # Both datasets have the same entity but with dataset-local run_ids.
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v2",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V2_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-hybrid-001",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:3907: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestStructuredAnchorCrossDatasetIsolation.test_no_cross_dataset_leakage_when_only_v1_exists _

self = <test_entity_resolution.TestStructuredAnchorCrossDatasetIsolation testMethod=test_no_cross_dataset_leakage_when_only_v1_exists>

    def test_no_cross_dataset_leakage_when_only_v1_exists(self):
        """When dataset_id=v2 and only v1 canonical entities exist, no resolution occurs."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            # Only v1 canonical node exists; v2 has none.
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-sa-003",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:4243: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestStructuredAnchorCrossDatasetIsolation.test_v2_canonical_query_is_scoped_by_dataset_id _

self = <test_entity_resolution.TestStructuredAnchorCrossDatasetIsolation testMethod=test_v2_canonical_query_is_scoped_by_dataset_id>

    def test_v2_canonical_query_is_scoped_by_dataset_id(self):
        """The CanonicalEntity lookup query must include dataset_id = v2 in its parameters."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v2",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V2_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-sa-002",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:4190: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestStructuredAnchorCrossDatasetIsolation.test_v2_structured_anchor_resolves_to_v2_canonical_not_v1_for_shared_qid _

self = <test_entity_resolution.TestStructuredAnchorCrossDatasetIsolation testMethod=test_v2_structured_anchor_resolves_to_v2_canonical_not_v1_for_shared_qid>

    def test_v2_structured_anchor_resolves_to_v2_canonical_not_v1_for_shared_qid(self):
        """v2 structured_anchor resolution must resolve to v2 CanonicalEntity nodes.
    
        When both demo_dataset_v1 and demo_dataset_v2 contain a CanonicalEntity for
        the same QID (e.g. Q950419 / Mercado Libre), running structured_anchor
        resolution for v2 must produce RESOLVES_TO edges pointing at the v2 canonical
        entity, not the v1 one.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config(Path(tmpdir))
            mentions = [
                {"mention_id": "m1", "name": self._SHARED_NAME, "entity_type": "organization"},
            ]
            # Both datasets have the same entity but with dataset-local run_ids.
            all_canonicals = [
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v1",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V1_DATASET,
                },
                {
                    "entity_id": self._SHARED_QID,
                    "run_id": "structured-run-v2",
                    "name": self._SHARED_NAME,
                    "aliases": None,
                    "dataset_id": self._V2_DATASET,
                },
            ]
            driver = self._make_cross_dataset_driver(mentions, all_canonicals, self._V2_DATASET)
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="cross-ds-sa-001",
                    source_uri=None,
                    dataset_id=self._V2_DATASET,
                )

demo/tests/test_entity_resolution.py:4125: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_hybrid_alignment_restored_after_dataset_id_repair _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_hybrid_alignment_restored_after_dataset_id_repair>

    def test_hybrid_alignment_restored_after_dataset_id_repair(self) -> None:
        """Stamping dataset_id on legacy canonical nodes (simulated repair) restores alignment.
    
        This test mirrors the in-place repair documented in
        docs/architecture/legacy-dataset-id-migration-v0.1.md §4 Path B.
        Before repair: zero aligned clusters.
        After repair (dataset_id set to the active dataset): at least one aligned cluster.
        """
        mentions = [self._mention()]
    
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_hybrid(Path(tmpdir))
    
            # --- Before repair: legacy node has dataset_id=None ---
            driver_before = _make_neo4j_test_driver(mentions, [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver_before):
>               result_before = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-repair-before",
                    source_uri=None,
                    dataset_id=self._DATASET,
                )

demo/tests/test_entity_resolution.py:4402: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_hybrid_warning_mentions_legacy_null_when_zero_alignment _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_hybrid_warning_mentions_legacy_null_when_zero_alignment>

    def test_hybrid_warning_mentions_legacy_null_when_zero_alignment(self) -> None:
        """Warning emitted for zero hybrid alignment must mention the legacy null scenario.
    
        The warning text should guide operators to the migration document rather
        than only suggesting they re-run structured ingest.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_hybrid(Path(tmpdir))
            driver = _make_neo4j_test_driver([self._mention()], [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-hybrid-002",
                    source_uri=None,
                    dataset_id=self._DATASET,
                )

demo/tests/test_entity_resolution.py:4369: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_hybrid_zero_alignment_when_only_legacy_null_nodes_exist _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_hybrid_zero_alignment_when_only_legacy_null_nodes_exist>

    def test_hybrid_zero_alignment_when_only_legacy_null_nodes_exist(self) -> None:
        """Hybrid mode produces zero aligned clusters when canonical nodes have dataset_id=null.
    
        This is the symptom operators observe after upgrading to a release that
        includes the strict dataset_id filter (PR #466), when their graph still
        contains pre-stamping CanonicalEntity nodes.
        """
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_hybrid(Path(tmpdir))
            driver = _make_neo4j_test_driver([self._mention()], [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-hybrid-001",
                    source_uri=None,
                    dataset_id=self._DATASET,
                )

demo/tests/test_entity_resolution.py:4346: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_structured_anchor_resolution_restored_after_dataset_id_repair _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_structured_anchor_resolution_restored_after_dataset_id_repair>

    def test_structured_anchor_resolution_restored_after_dataset_id_repair(self) -> None:
        """Stamping dataset_id restores structured_anchor resolution after a legacy-null scenario."""
        mentions = [self._mention()]
    
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_structured_anchor(Path(tmpdir))
            driver_before = _make_neo4j_test_driver(mentions, [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver_before):
>               result_before = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-sa-repair-before",
                    source_uri=None,
                    dataset_id=self._DATASET,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:4485: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_structured_anchor_warning_mentions_legacy_null_when_zero_resolution _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_structured_anchor_warning_mentions_legacy_null_when_zero_resolution>

    def test_structured_anchor_warning_mentions_legacy_null_when_zero_resolution(self) -> None:
        """Warning emitted for zero structured_anchor resolution must mention legacy null nodes."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_structured_anchor(Path(tmpdir))
            driver = _make_neo4j_test_driver([self._mention()], [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-sa-002",
                    source_uri=None,
                    dataset_id=self._DATASET,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:4463: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
_ TestLegacyNullDatasetIdBehavior.test_structured_anchor_zero_resolution_when_only_legacy_null_nodes_exist _

self = <test_entity_resolution.TestLegacyNullDatasetIdBehavior testMethod=test_structured_anchor_zero_resolution_when_only_legacy_null_nodes_exist>

    def test_structured_anchor_zero_resolution_when_only_legacy_null_nodes_exist(self) -> None:
        """structured_anchor mode resolves zero mentions when canonical nodes have dataset_id=null."""
        with tempfile.TemporaryDirectory() as tmpdir:
            config = self._live_config_structured_anchor(Path(tmpdir))
            driver = _make_neo4j_test_driver([self._mention()], [self._legacy_canonical()])
            with patch("neo4j.GraphDatabase.driver", return_value=driver):
>               result = _run_entity_resolution_via_request_context(
                    config,
                    run_id="legacy-sa-001",
                    source_uri=None,
                    dataset_id=self._DATASET,
                    resolution_mode="structured_anchor",
                )

demo/tests/test_entity_resolution.py:4443: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
demo/tests/test_entity_resolution.py:144: in _run_entity_resolution_via_request_context
    return run_entity_resolution_request_context(
demo/stages/entity_resolution.py:565: in run_entity_resolution_request_context
    return _run_entity_resolution_request_context_impl(
src/power_atlas/entity_resolution_entrypoint.py:180: in run_entity_resolution_request_context
    return resolved_config_runner(
demo/stages/entity_resolution.py:471: in _run_entity_resolution_impl
    return _run_entity_resolution_impl_entrypoint(
src/power_atlas/entity_resolution_entrypoint.py:148: in run_entity_resolution
    return resolved_runtime_runner(
demo/stages/entity_resolution.py:505: in _run_entity_resolution_runtime
    return _run_entity_resolution_runtime_impl(
src/power_atlas/entity_resolution_runner.py:260: in run_entity_resolution_runtime
    live_result = live_runner(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

neo4j_settings = Neo4jSettings(uri='bolt://example.invalid', username='neo4j', password='secret', database='neo4j')

    def run_entity_resolution_live(
        neo4j_settings: Neo4jSettings,
        *,
        run_id: str,
        source_uri: str | None,
        resolution_mode: str,
        effective_dataset_id: str,
        alignment_version: str,
        neo4j_database: str | None,
        entity_resolution_alignment: EntityResolutionAlignmentContract | None,
        entity_resolution_canonical_lookup: EntityResolutionCanonicalLookupContract | None,
        entity_resolution_graph: EntityResolutionGraphContract | None,
        fetch_mentions: Callable[..., list[dict[str, Any]]],
        cluster_mentions: Callable[[list[dict[str, Any]]], list[dict[str, Any]]],
        fetch_canonicals: Callable[..., list[dict[str, Any]]],
        build_lookup_tables: Callable[..., tuple[dict[str, Any], dict[str, Any], dict[str, Any]]],
        make_cluster_id: Callable[[str, str | None, str], str],
        align_clusters_to_canonical: Callable[..., list[dict[str, Any]]],
        resolve_mention: Callable[..., dict[str, Any]],
        write_resolution_results: Callable[..., None],
        write_alignment_results: Callable[..., None],
        fetch_member_of_coverage: Callable[..., Any],
        fetch_alignment_coverage: Callable[..., Any],
    ) -> EntityResolutionLiveResult:
        graph_mentions_clustered = 0
        graph_mentions_unclustered = 0
        graph_total_clusters = 0
        graph_aligned_clusters = 0
        graph_distinct_canonical_entities = 0
        graph_mentions_in_aligned = 0
        graph_alignment_breakdown: dict[str, int] = {}
        stage_warnings: list[str] = []
    
        with create_neo4j_driver(neo4j_settings) as driver:
            mentions = fetch_mentions(
                driver,
                run_id=run_id,
                source_uri_fallback=source_uri,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
    
            resolved_rows: list[dict[str, Any]] = []
            unresolved_rows: list[dict[str, Any]] = []
            resolution_breakdown: dict[str, int] = {}
            alignment_rows: list[dict[str, Any]] = []
    
            if resolution_mode == "unstructured_only":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
            elif resolution_mode == "hybrid":
                cluster_rows_result = cluster_mentions(mentions)
                for row in cluster_rows_result:
                    method = row["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
                    unresolved_rows.append(row)
    
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(hybrid alignment skipped); check that structured ingest has run for this dataset "
                        f"and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
                if canonical_nodes:
                    _, by_label, by_alias = build_lookup_tables(canonical_nodes)
                    cluster_entries_by_id: dict[str, tuple[tuple[str, str], dict[str, Any]]] = {}
                    for row in unresolved_rows:
                        cluster_id = make_cluster_id(run_id, row.get("entity_type"), row["normalized_text"])
                        if cluster_id not in cluster_entries_by_id:
                            sort_key = (row.get("entity_type") or "", row["normalized_text"])
                            cluster_entries_by_id[cluster_id] = (
                                sort_key,
                                {
                                    "cluster_id": cluster_id,
                                    "normalized_text": row["normalized_text"],
                                },
                            )
                    unique_clusters = [
                        cluster
                        for _, cluster in sorted(cluster_entries_by_id.values(), key=lambda item: item[0])
                    ]
                    alignment_rows = align_clusters_to_canonical(unique_clusters, by_label, by_alias)
            else:
                canonical_nodes = fetch_canonicals(
                    driver,
                    dataset_id=effective_dataset_id,
                    neo4j_database=neo4j_database,
                    entity_resolution_canonical_lookup=entity_resolution_canonical_lookup,
                    entity_resolution_graph=entity_resolution_graph,
                )
                if not canonical_nodes:
                    canonical_label = (
                        entity_resolution_graph.canonical_label
                        if entity_resolution_graph is not None
                        else "CanonicalEntity"
                    )
                    stage_warnings.append(
                        f"{canonical_label} lookup returned zero rows for dataset_id={effective_dataset_id!r} "
                        f"(all mentions will be unresolved); check that structured ingest has run for this "
                        f"dataset and that {canonical_label} nodes carry a matching dataset_id property.  "
                        f"If {canonical_label} nodes already exist but have dataset_id=null (legacy graph), "
                        f"run the in-place repair Cypher or re-ingest from the structured fixture — "
                        f"see docs/architecture/legacy-dataset-id-migration-v0.1.md."
                    )
    
                by_qid, by_label, by_alias = build_lookup_tables(canonical_nodes)
    
                for mention in mentions:
                    result_rec = resolve_mention(mention, by_qid, by_label, by_alias)
                    method = result_rec["resolution_method"]
                    resolution_breakdown[method] = resolution_breakdown.get(method, 0) + 1
    
                    if result_rec["resolved"]:
                        resolved_rows.append(result_rec)
                    else:
                        unresolved_rows.append(result_rec)
    
>           write_resolution_results(
                driver,
                run_id=run_id,
                source_uri=source_uri,
                resolved_rows=resolved_rows,
                unresolved_rows=unresolved_rows,
                neo4j_database=neo4j_database,
                entity_resolution_graph=entity_resolution_graph,
            )
E           TypeError: _write_resolution_results() got an unexpected keyword argument 'entity_resolution_graph'

src/power_atlas/adapters/neo4j/entity_resolution_runtime.py:165: TypeError
___ test_apply_provenance_prefers_document_metadata_and_propagates_to_chunks ___

    def test_apply_provenance_prefers_document_metadata_and_propagates_to_chunks():
        module, fake_types = _load_provenance_writer()
        ProvenanceNeo4jWriter = module.ProvenanceNeo4jWriter
        LexicalGraphConfig = fake_types.LexicalGraphConfig
        Node = fake_types.Node
        Relationship = fake_types.Relationship
        Neo4jGraph = fake_types.Neo4jGraph
    
        config = LexicalGraphConfig(
            document_node_label="Document",
            chunk_node_label="Chunk",
            chunk_to_document_relationship_type="FROM_DOCUMENT",
        )
        doc1 = Node(
            "d1",
            "Document",
            {
                "metadata": {"run_id": "stage-run", "dataset_id": "doc-ds", "source_uri": "doc-src"},
                "path": "ignored-path",
            },
        )
        doc2 = Node("d2", "Document", {"path": "file:///doc2.pdf"})
        chunk1 = Node("c1", "Chunk", {})
        chunk2 = Node("c2", "Chunk", {})
        chunk3 = Node("c3", "Chunk", {})
        rel1 = Relationship("c1", "d1", "FROM_DOCUMENT")
        rel2 = Relationship("c2", "d2", "FROM_DOCUMENT")
        graph = Neo4jGraph(nodes=[doc1, doc2, chunk1, chunk2, chunk3], relationships=[rel1, rel2])
    
>       writer = ProvenanceNeo4jWriter(driver=None, dataset_id="writer-ds")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

demo/tests/test_provenance_writer.py:152: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
src/power_atlas/neo4j_io.py:154: in __init__
    super().__init__(driver=driver, neo4j_database=neo4j_database)
vendor/neo4j-graphrag-python/src/neo4j_graphrag/experimental/components/kg_writer.py:201: in __init__
    self.driver = driver_config.override_user_agent(driver)
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

driver = None

    def override_user_agent(driver: neo4j.Driver) -> neo4j.Driver:
>       driver._pool.pool_config.user_agent = f"neo4j-graphrag-python/v{__version__}"
        ^^^^^^^^^^^^
E       AttributeError: 'NoneType' object has no attribute '_pool'

vendor/neo4j-graphrag-python/src/neo4j_graphrag/utils/driver_config.py:21: AttributeError
__________ TestBuildCanonicalNamesExpr.test_run_scoped_exact_fragment __________

self = <test_retrieval_query_builders.TestBuildCanonicalNamesExpr object at 0x1170b9d10>

    def test_run_scoped_exact_fragment(self) -> None:
        result = _build_canonical_names_expr(run_scoped=True)
>       assert result == (
            "[(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[:RESOLVES_TO]->(canonical)"
            " WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities"
        )
E       AssertionError: assert '[(c)<-[:MENT...ical_entities' == '[(c)<-[:MENT...ical_entities'
E         
E         Skipping 62 identical leading characters in diff, use -v to show
E         - (canonical) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities
E         + (canonical:CanonicalEntity) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities
E         ?           ++++++++++++++++

demo/tests/test_retrieval_query_builders.py:145: AssertionError
___________ TestBuildCanonicalNamesExpr.test_all_runs_exact_fragment ___________

self = <test_retrieval_query_builders.TestBuildCanonicalNamesExpr object at 0x1170b6350>

    def test_all_runs_exact_fragment(self) -> None:
        result = _build_canonical_names_expr(run_scoped=False)
>       assert result == (
            "[(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[:RESOLVES_TO]->(canonical)"
            " | canonical.name] AS canonical_entities"
        )
E       AssertionError: assert '[(c)<-[:MENT...ical_entities' == '[(c)<-[:MENT...ical_entities'
E         
E         Skipping 62 identical leading characters in diff, use -v to show
E         - (canonical) | canonical.name] AS canonical_entities
E         + (canonical:CanonicalEntity) | canonical.name] AS canonical_entities
E         ?           ++++++++++++++++

demo/tests/test_retrieval_query_builders.py:152: AssertionError
____ TestBuildClusterCanonicalAlignmentsExpr.test_run_scoped_exact_fragment ____

self = <test_retrieval_query_builders.TestBuildClusterCanonicalAlignmentsExpr object at 0x1170c2a90>

    def test_run_scoped_exact_fragment(self) -> None:
        result = _build_cluster_canonical_alignments_expr(run_scoped=True)
>       assert result == (
            "[(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[:MEMBER_OF]"
            "->(cluster:ResolvedEntityCluster)-[a:ALIGNED_WITH]->(aligned_canonical)"
            " WHERE mention.run_id = $run_id AND a.run_id = $run_id"
            " AND a.alignment_version = $alignment_version"
            " | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method,"
            " alignment_status: a.alignment_status}] AS cluster_canonical_alignments"
        )
E       AssertionError: assert '[(c)<-[:MENT...al_alignments' == '[(c)<-[:MENT...al_alignments'
E         
E         Skipping 118 identical leading characters in diff, use -v to show
E         - _canonical) WHERE mention.run_id = $run_id AND a.run_id = $run_id AND a.alignment_version = $alignment_version | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method, alignment_status: a.alignment_status}] AS cluster_canonical_alignments
E         + _canonical:CanonicalEntity) WHERE mention.run_id = $run_id AND a.run_id = $run_id AND a.alignment_version = $alignment_version | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method, alignment_status: a.alignment_status}] AS cluster_canonical_alignments
E         ?           ++++++++++++++++

demo/tests/test_retrieval_query_builders.py:249: AssertionError
_____ TestBuildClusterCanonicalAlignmentsExpr.test_all_runs_exact_fragment _____

self = <test_retrieval_query_builders.TestBuildClusterCanonicalAlignmentsExpr object at 0x1170c3090>

    def test_all_runs_exact_fragment(self) -> None:
        result = _build_cluster_canonical_alignments_expr(run_scoped=False)
>       assert result == (
            "[(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[:MEMBER_OF]"
            "->(cluster:ResolvedEntityCluster)-[a:ALIGNED_WITH]->(aligned_canonical)"
            " WHERE a.run_id = mention.run_id AND a.alignment_version = $alignment_version"
            " | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method,"
            " alignment_status: a.alignment_status}] AS cluster_canonical_alignments"
        )
E       AssertionError: assert '[(c)<-[:MENT...al_alignments' == '[(c)<-[:MENT...al_alignments'
E         
E         Skipping 118 identical leading characters in diff, use -v to show
E         - _canonical) WHERE a.run_id = mention.run_id AND a.alignment_version = $alignment_version | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method, alignment_status: a.alignment_status}] AS cluster_canonical_alignments
E         + _canonical:CanonicalEntity) WHERE a.run_id = mention.run_id AND a.alignment_version = $alignment_version | {canonical_name: aligned_canonical.name, alignment_method: a.alignment_method, alignment_status: a.alignment_status}] AS cluster_canonical_alignments
E         ?           ++++++++++++++++

demo/tests/test_retrieval_query_builders.py:260: AssertionError
___ TestBuildRetrievalQueryContractSnapshots.test_expansion_matches_snapshot ___

self = <test_retrieval_query_builders.TestBuildRetrievalQueryContractSnapshots object at 0x1170c2850>

    def test_expansion_matches_snapshot(self) -> None:
>       assert _build_retrieval_query(expand_graph=True) == _SNAPSHOT_WITH_EXPANSION
E       AssertionError: assert '\nWITH node ...aim_details\n' == '\nWITH node ...aim_details\n'
E         
E         Skipping 946 identical leading characters in diff, use -v to show
E         - (canonical) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities,
E         + (canonical:CanonicalEntity) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities,
E         ?           ++++++++++++++++
E                  claim_details

demo/tests/test_retrieval_query_builders.py:423: AssertionError
_ TestBuildRetrievalQueryContractSnapshots.test_expansion_all_runs_matches_snapshot _

self = <test_retrieval_query_builders.TestBuildRetrievalQueryContractSnapshots object at 0x1170c1c50>

    def test_expansion_all_runs_matches_snapshot(self) -> None:
>       assert (
            _build_retrieval_query(expand_graph=True, all_runs=True)
            == _SNAPSHOT_WITH_EXPANSION_ALL_RUNS
        )
E       AssertionError: assert '\nWITH node ...aim_details\n' == '\nWITH node ...aim_details\n'
E         
E         Skipping 861 identical leading characters in diff, use -v to show
E         - (canonical) | canonical.name] AS canonical_entities,
E         + (canonical:CanonicalEntity) | canonical.name] AS canonical_entities,
E         ?           ++++++++++++++++
E                  claim_details

demo/tests/test_retrieval_query_builders.py:426: AssertionError
____ TestBuildRetrievalQueryContractSnapshots.test_cluster_matches_snapshot ____

self = <test_retrieval_query_builders.TestBuildRetrievalQueryContractSnapshots object at 0x1170c36d0>

    def test_cluster_matches_snapshot(self) -> None:
>       assert _build_retrieval_query(cluster_aware=True) == _SNAPSHOT_WITH_CLUSTER
E       AssertionError: assert '\nWITH node ..._alignments\n' == '\nWITH node ..._alignments\n'
E         
E         Skipping 946 identical leading characters in diff, use -v to show
E         - (canonical) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities,
E         + (canonical:CanonicalEntity) WHERE mention.run_id = $run_id | canonical.name] AS canonical_entities,
E         ?           ++++++++++++++++
E                  claim_details,
E                  [(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[r:MEMBER_OF]->(cluster:ResolvedEntityCluster) WHERE mention.run_id = $run_id | {cluster_id: cluster.cluster_id, cluster_name: cluster.canonical_name, membership_status: r.status, membership_method: r.method}] AS cluster_memberships,...
E         
E         ...Full output truncated (3 lines hidden), use '-vv' to show

demo/tests/test_retrieval_query_builders.py:432: AssertionError
_ TestBuildRetrievalQueryContractSnapshots.test_cluster_all_runs_matches_snapshot _

self = <test_retrieval_query_builders.TestBuildRetrievalQueryContractSnapshots object at 0x1170c3a50>

    def test_cluster_all_runs_matches_snapshot(self) -> None:
>       assert (
            _build_retrieval_query(cluster_aware=True, all_runs=True)
            == _SNAPSHOT_WITH_CLUSTER_ALL_RUNS
        )
E       AssertionError: assert '\nWITH node ..._alignments\n' == '\nWITH node ..._alignments\n'
E         
E         Skipping 861 identical leading characters in diff, use -v to show
E         - (canonical) | canonical.name] AS canonical_entities,
E         + (canonical:CanonicalEntity) | canonical.name] AS canonical_entities,
E         ?           ++++++++++++++++
E                  claim_details,
E                  [(c)<-[:MENTIONED_IN]-(mention:EntityMention)-[r:MEMBER_OF]->(cluster:ResolvedEntityCluster) | {cluster_id: cluster.cluster_id, cluster_name: cluster.canonical_name, membership_status: r.status, membership_method: r.method}] AS cluster_memberships,...
E         
E         ...Full output truncated (3 lines hidden), use '-vv' to show

demo/tests/test_retrieval_query_builders.py:435: AssertionError
=========================== short test summary info ============================
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_empty_mentions_returns_zero_counts
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_resolves_alias_match
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_resolves_label_match
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_resolves_qid_match
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_unresolved_fallback
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_uses_config_dataset_name_when_dataset_id_omitted
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_writes_summary_artifact
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextLive::test_live_writes_unresolved_artifact
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_clusters_created_counts_unique_entity_type_and_text_pairs
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_clusters_created_none_and_empty_entity_type_counted_as_one
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_cross_run_clusters_created_are_isolated
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_clusters_created_equals_unique_normalized_texts
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_clusters_created_is_zero_when_all_resolved
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_member_of_edge_written_for_unresolved
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_resolved_entity_cluster_node_merged
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_summary_carries_cluster_version
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_live_unresolved_artifact_includes_cluster_id
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_per_mention_source_uri_produces_same_cluster_id_in_artifact
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_per_mention_source_uri_retained_on_member_of_edges
FAILED demo/tests/test_entity_resolution.py::TestResolvedEntityCluster::test_unresolved_artifact_includes_entity_type
FAILED demo/tests/test_entity_resolution.py::TestEntityTypeDriftReport::test_live_summary_includes_entity_type_report
FAILED demo/tests/test_entity_resolution.py::TestEntityTypeDriftReport::test_sentinel_label_collision_propagates_to_stage_warnings
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_abbreviation_method_writes_candidate_status
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_borderline_fuzzy_writes_review_required_status
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_candidate_match_edges_written_for_abbreviation
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_candidate_match_edges_written_for_borderline_fuzzy
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_deterministic_methods_write_accepted_status
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_fuzzy_method_writes_provisional_status
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_all_mentions_clustered
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_clustering_invariant
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_mentions_clustered_equals_mentions_total
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_no_canonical_lookup
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_normalized_exact_reduces_clusters
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_resolution_breakdown_no_structured_methods
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_live_resolution_mode_in_summary
FAILED demo/tests/test_entity_resolution.py::TestClusterMentionsUnstructuredOnly::test_no_candidate_match_edges_for_accepted_memberships
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_aligned_with_edge_written_for_label_match
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_aligned_with_written_for_alias_match
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_aligned_with_written_with_non_null_source_uri
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_alignment_breakdown_reflects_methods
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_alignment_version_in_summary
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_all_mentions_become_unresolved
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_clustering_invariant
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_clusters_pending_alignment_zero_when_all_align
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_distinct_canonical_entities_aligned
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_distinct_canonical_entities_aligned_multiple
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_does_not_create_resolves_to_edges
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_member_of_edge_written
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_mentions_clustered_equals_mentions_total
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_mentions_in_aligned_clusters
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_no_aligned_clusters_when_no_canonical
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_resolution_mode_in_summary
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_resolver_method_in_summary
FAILED demo/tests/test_entity_resolution.py::TestEntityResolutionRequestContextHybrid::test_live_unaligned_clusters_preserved
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_hybrid_aligned_clusters_nonzero_when_alignments_exist
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_hybrid_alignment_bounds_invariant
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_hybrid_alignment_partition_invariant
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_hybrid_all_mentions_clustered_at_scale
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_hybrid_manifest_reflects_graph_at_live_run_scale
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_unstructured_only_all_mentions_clustered_at_scale
FAILED demo/tests/test_entity_resolution.py::TestManifestGraphConsistency::test_unstructured_only_clusters_created_at_most_mentions_total
FAILED demo/tests/test_entity_resolution.py::TestHybridAlignmentCrossDatasetIsolation::test_no_cross_dataset_leakage_when_only_v1_exists
FAILED demo/tests/test_entity_resolution.py::TestHybridAlignmentCrossDatasetIsolation::test_v2_canonical_query_is_scoped_by_dataset_id
FAILED demo/tests/test_entity_resolution.py::TestHybridAlignmentCrossDatasetIsolation::test_v2_hybrid_aligns_to_v2_canonical_not_v1_for_shared_qid
FAILED demo/tests/test_entity_resolution.py::TestStructuredAnchorCrossDatasetIsolation::test_no_cross_dataset_leakage_when_only_v1_exists
FAILED demo/tests/test_entity_resolution.py::TestStructuredAnchorCrossDatasetIsolation::test_v2_canonical_query_is_scoped_by_dataset_id
FAILED demo/tests/test_entity_resolution.py::TestStructuredAnchorCrossDatasetIsolation::test_v2_structured_anchor_resolves_to_v2_canonical_not_v1_for_shared_qid
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_hybrid_alignment_restored_after_dataset_id_repair
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_hybrid_warning_mentions_legacy_null_when_zero_alignment
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_hybrid_zero_alignment_when_only_legacy_null_nodes_exist
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_structured_anchor_resolution_restored_after_dataset_id_repair
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_structured_anchor_warning_mentions_legacy_null_when_zero_resolution
FAILED demo/tests/test_entity_resolution.py::TestLegacyNullDatasetIdBehavior::test_structured_anchor_zero_resolution_when_only_legacy_null_nodes_exist
FAILED demo/tests/test_provenance_writer.py::test_apply_provenance_prefers_document_metadata_and_propagates_to_chunks
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildCanonicalNamesExpr::test_run_scoped_exact_fragment
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildCanonicalNamesExpr::test_all_runs_exact_fragment
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildClusterCanonicalAlignmentsExpr::test_run_scoped_exact_fragment
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildClusterCanonicalAlignmentsExpr::test_all_runs_exact_fragment
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildRetrievalQueryContractSnapshots::test_expansion_matches_snapshot
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildRetrievalQueryContractSnapshots::test_expansion_all_runs_matches_snapshot
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildRetrievalQueryContractSnapshots::test_cluster_matches_snapshot
FAILED demo/tests/test_retrieval_query_builders.py::TestBuildRetrievalQueryContractSnapshots::test_cluster_all_runs_matches_snapshot
82 failed, 2071 passed, 3 subtests passed in 21.77s
